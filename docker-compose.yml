services:
  mongo:
    image: mongo:7
    container_name: cooptech-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: cooptech

volumes:
  mongo_data:
